# J'ai crée un alias pour executer ce script

# En effet, j'ai exécuté la commande suivante : alias ppp='./sudo gitpushauto.sh'

# Il suffira donc d'exécuter ppp pour lancer le script

# Je pourrais supprimer cet alias en faisant : unalias ppp

# Pour voir les alias déjà en place : alias 


sudo git add .
sudo git commit -m "first commit"
sudo git push -u origin main
